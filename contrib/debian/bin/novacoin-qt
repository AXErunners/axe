#!/bin/sh

set -e

umask 077

basedir=~/.axe
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/axe.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# axe does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/axe/axe-qt "$@"
